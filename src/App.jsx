import React,{useEffect,useMemo,useRef,useState}from"react";
import{Bell,Camera,Check,ChevronLeft,ChevronRight,CircleAlert,Download,Edit3,Home,ImagePlus,LayoutDashboard,Map,MapPin,Maximize2,Minus,PawPrint,Plus,QrCode,Save,ScanLine,Settings,Shield,Trash2,UserRound,Users,Utensils,X}from"lucide-react";
const seed=[
{id:1,name:"HaySev Topluluk Odası",area:"Yakut Çarşı karşısı",x:61.3,y:26.5,status:"full",updated:Date.now()-2*3600000,animals:"8-12 kedi",text:"HaySev kulübesinin yanındaki ağaçlık alan."},
{id:2,name:"İletişim Fakültesi Bahçesi",area:"İletişim Fakültesi",x:29.2,y:46.5,status:"warning",updated:Date.now()-31*3600000,animals:"6-9 kedi",text:"Fakülte girişinin sol tarafındaki yeşil alan."},
{id:3,name:"KYK Kız Yurtları Noktası",area:"Yurtlar Bölgesi",x:55.8,y:75.5,status:"urgent",updated:Date.now()-55*3600000,animals:"10-15 kedi",text:"Yurt kümesinin kuzey girişindeki yürüyüş yolu yanı."}];
const S={full:["Dolu","#0A8F5B"],warning:["Azalıyor","#FFB000"],urgent:["Kontrol","#7146D9"],empty:["Boş olabilir","#F04444"]};
const ago=t=>{let h=Math.max(0,Math.floor((Date.now()-t)/3600000));return h<1?"Şimdi":h<24?`${h} saat önce`:`${Math.floor(h/24)} gün önce`};
function MapView({image,points,onPoint,admin,onAdd,onExpand,fullscreen=false}){const [z,setZ]=useState(fullscreen?1.15:1);const [p,setP]=useState({x:0,y:0});const drag=useRef(null);function down(e){drag.current={startX:e.clientX,startY:e.clientY,baseX:p.x,baseY:p.y};e.currentTarget.setPointerCapture?.(e.pointerId)}function move(e){if(!drag.current)return;setP({x:drag.current.baseX+(e.clientX-drag.current.startX),y:drag.current.baseY+(e.clientY-drag.current.startY)})}function end(e){drag.current=null;e.currentTarget.releasePointerCapture?.(e.pointerId)}
return <div className={`map ${fullscreen?"mapFullscreen":""}`} onPointerDown={down} onPointerMove={move} onPointerUp={end} onPointerCancel={end} onDoubleClick={e=>{if(!admin)return;let r=e.currentTarget.getBoundingClientRect();onAdd({x:+(((e.clientX-r.left)/r.width)*100).toFixed(1),y:+(((e.clientY-r.top)/r.height)*100).toFixed(1)})}}><div className="mapInner" style={{transform:`translate(${p.x}px,${p.y}px) scale(${z})`}}><img src={image}/>{points.map(q=><button key={q.id} className="pin" style={{left:q.x+'%',top:q.y+'%',background:S[q.status][1]}} onPointerDown={e=>e.stopPropagation()} onClick={()=>onPoint(q)}><PawPrint size={15}/></button>)}</div><div className="zoom">{onExpand&&<button aria-label="Haritayı tam ekran aç" onClick={onExpand}><Maximize2/></button>}<button onClick={()=>setZ(v=>Math.min(3,v+.25))}><Plus/></button><button onClick={()=>setZ(v=>Math.max(1,v-.25))}><Minus/></button></div>{admin&&<div className="mapHint"><MapPin size={15}/> Nokta eklemek için haritaya çift tıkla</div>}</div>}
function Sheet({point,onClose,onUpdate}){if(!point)return null;return <><button className="shade" onClick={onClose}/><section className="sheet"><div className="handle"/><button className="close" onClick={onClose}><X/></button><span className="badge" style={{color:S[point.status][1]}}>{S[point.status][0]} · {ago(point.updated)}</span><h2>{point.name}</h2><p><MapPin size={14}/> {point.area}</p><div className="info"><b><PawPrint/> {point.animals}</b><b><Utensils/> Mama noktası</b></div><div className="note">{point.text}</div><h3>Hızlı güncelleme</h3><div className="actions"><button className="primary" onClick={()=>onUpdate('full')}><Utensils/> Mama bıraktım</button><button onClick={()=>onUpdate('full')}><Check/> Mama hâlâ var</button><button onClick={()=>onUpdate('empty')}><CircleAlert/> Mama bitmiş</button><button><Camera/> Fotoğraf ekle</button></div></section></>}
function Admin({points,setPoints,image,setImage,onBack}){const [selected,setSelected]=useState(null);const [draft,setDraft]=useState(null);const input=useRef();function add(pos){setDraft({id:Date.now(),name:"Yeni Mama Noktası",area:"Kampüs",status:"full",updated:Date.now(),animals:"Hayvan sayısı girilmedi",text:"Konum tarifi ekleyin.",...pos})}function save(){if(!draft)return;setPoints(v=>v.some(x=>x.id===draft.id)?v.map(x=>x.id===draft.id?draft:x):[...v,draft]);setDraft(null)}function file(e){let f=e.target.files?.[0];if(!f)return;let r=new FileReader();r.onload=()=>{setImage(r.result);localStorage.setItem('aph-image',r.result)};r.readAsDataURL(f)}
return <div className="admin"><header className="adminHead"><button onClick={onBack}><ChevronLeft/></button><div><small>YÖNETİCİ PANELİ</small><h1>Pati kontrol merkezi</h1></div><Shield/></header><div className="adminStats"><div><b>{points.length}</b><span>Toplam nokta</span></div><div><b>{points.filter(x=>x.status==='empty'||x.status==='urgent').length}</b><span>Kontrol</span></div><div><b>{points.filter(x=>Date.now()-x.updated<86400000).length}</b><span>Bugün</span></div></div><div className="adminMap"><MapView image={image} points={points} admin onAdd={add} onPoint={q=>setDraft({...q})}/></div><div className="toolbar"><button onClick={()=>input.current.click()}><ImagePlus/> Haritayı değiştir</button><input ref={input} hidden type="file" accept="image/*" onChange={file}/><button onClick={()=>add({x:50,y:50})}><Plus/> Nokta ekle</button></div><section className="panel"><h2>Mama noktaları</h2>{points.map(q=><article key={q.id}><i style={{background:S[q.status][1]}}/><div><b>{q.name}</b><small>{q.area} · {ago(q.updated)}</small></div><button onClick={()=>setDraft({...q})}><Edit3/></button><button onClick={()=>setPoints(v=>v.filter(x=>x.id!==q.id))}><Trash2/></button></article>)}</section>{draft&&<div className="editor"><div className="editorHead"><h2>Nokta bilgileri</h2><button onClick={()=>setDraft(null)}><X/></button></div><label>Nokta adı<input value={draft.name} onChange={e=>setDraft({...draft,name:e.target.value})}/></label><label>Bölge<input value={draft.area} onChange={e=>setDraft({...draft,area:e.target.value})}/></label><label>Hayvan bilgisi<input value={draft.animals} onChange={e=>setDraft({...draft,animals:e.target.value})}/></label><label>Konum tarifi<textarea value={draft.text} onChange={e=>setDraft({...draft,text:e.target.value})}/></label><label>Durum<select value={draft.status} onChange={e=>setDraft({...draft,status:e.target.value})}>{Object.entries(S).map(([k,v])=><option value={k}>{v[0]}</option>)}</select></label><div className="coords">Harita konumu: %{draft.x} / %{draft.y}</div><button className="save" onClick={save}><Save/> Kaydet</button></div>}</div>}
function CameraScanner({onClose,onResult}){
const video=useRef(null),stream=useRef(null);const[error,setError]=useState("");const[result,setResult]=useState("");
useEffect(()=>{let active=true,timer;async function start(){try{if(!navigator.mediaDevices?.getUserMedia)throw new Error("Bu tarayıcı kamera erişimini desteklemiyor.");stream.current=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:"environment"}},audio:false});if(!active)return;video.current.srcObject=stream.current;await video.current.play();if("BarcodeDetector" in window){const detector=new BarcodeDetector({formats:["qr_code"]});const tick=async()=>{if(!active)return;try{const codes=await detector.detect(video.current);if(codes[0]?.rawValue){setResult(codes[0].rawValue);onResult?.(codes[0].rawValue);return}}catch{}timer=setTimeout(tick,350)};tick()}}catch(e){setError(e.name==="NotAllowedError"?"Kamera izni verilmedi. Adres çubuğundaki kilit simgesinden kamera iznini aç.":e.message)}}start();return()=>{active=false;clearTimeout(timer);stream.current?.getTracks().forEach(t=>t.stop())}},[]);
return <div className="scanner"><button onClick={onClose}><X/></button><h2>QR kodu tara</h2><p>Arka kamera otomatik açılır.</p><div className="cameraBox">{!error&&<video ref={video} playsInline muted/>}<span className="scanLine"/>{error&&<div className="cameraError"><Camera/><b>Kamera açılamadı</b><small>{error}</small></div>}</div>{result&&<div className="scanResult"><Check/> QR okundu: {result}</div>}<p className="cameraTip">Vercel adresinde HTTPS üzerinden kamera izni vermen gerekir.</p></div>}
export default function App(){const[page,setPage]=useState('home');const[points,setPoints]=useState(()=>JSON.parse(localStorage.getItem('aph-points')||'null')||seed);const[image,setImage]=useState(()=>localStorage.getItem('aph-image')||`${import.meta.env.BASE_URL}kampus-haritasi.png`);const[selected,setSelected]=useState(null);const[scan,setScan]=useState(false);const[full,setFull]=useState(false);useEffect(()=>localStorage.setItem('aph-points',JSON.stringify(points)),[points]);useEffect(()=>{document.body.style.overflow=full||scan?'hidden':'';return()=>{document.body.style.overflow=''}},[full,scan]);const counts=useMemo(()=>({today:points.filter(x=>Date.now()-x.updated<86400000).length,control:points.filter(x=>x.status==='urgent').length,empty:points.filter(x=>x.status==='empty').length}),[points]);if(page==='admin')return <Admin {...{points,setPoints,image,setImage}} onBack={()=>setPage('home')}/>;return <main><div className="phone"><header><div><small>AKDENİZ ÜNİVERSİTESİ</small><h1>Pati Haritası</h1><p>Kampüsteki dostlarımız için birlikte.</p></div><button><Bell/></button></header><div className="stats">{[[points.length,'Nokta'],[counts.today,'Bugün'],[counts.control,'Kontrol'],[counts.empty,'Acil']].map(x=><div><b>{x[0]}</b><span>{x[1]}</span></div>)}</div><section className="mapSec"><div className="title"><div><h2>Kampüs haritası</h2><p>Pati işaretlerine dokunarak incele</p></div><button onClick={()=>setPage('admin')}><Settings/> Yönet</button></div><MapView image={image} points={points} onPoint={setSelected} onExpand={()=>setFull(true)}/></section><section><h2>Hızlı işlemler</h2><div className="quick"><button onClick={()=>setScan(true)}><ScanLine/><b>QR kodu tara</b><span>Noktayı hızlı güncelle</span></button><button><CircleAlert/><b>Acil noktalar</b><span>Öncelikli alanları gör</span></button></div></section><nav>{[[Home,'Ana'],[Map,'Harita'],[QrCode,'QR'],[CircleAlert,'Acil'],[UserRound,'Profil']].map(([I,t])=><button onClick={()=>{if(t==='QR')setScan(true);if(t==='Harita')setFull(true)}}><I/><span>{t}</span></button>)}</nav><Sheet point={selected} onClose={()=>setSelected(null)} onUpdate={s=>{setPoints(v=>v.map(x=>x.id===selected.id?{...x,status:s,updated:Date.now()}:x));setSelected(null)}}/>{full&&<div className="fullOverlay"><button className="fullClose" onClick={()=>setFull(false)}><X/></button><MapView fullscreen image={image} points={points} onPoint={setSelected}/></div>}{scan&&<CameraScanner onClose={()=>setScan(false)} onResult={()=>setTimeout(()=>setScan(false),1200)}/>}</div></main>}
